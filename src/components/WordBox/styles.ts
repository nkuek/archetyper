import { makeStyles } from '@mui/styles';

const useStyles = makeStyles({
  correct: {
    color: 'green',
  },
  incorrect: {
    color: 'red',
  },
  currentWord: {
    color: 'plum',
  },
  currentChar: {
    color: 'darkmagenta',
  },
});

export default useStyles;
